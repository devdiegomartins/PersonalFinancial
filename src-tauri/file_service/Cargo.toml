[package]
name = "file_service"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "2.0.1"
crypt_service = { path = "../crypt_service" }
directories = "5.0"

[dev-dependencies]
tempfile = "3.8"
